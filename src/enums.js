const ProcessMode = {
  PUSH: "PROCESS_MODE_PUSH",
  PULL: "PROCESS_MODE_PULL",
  AUTO: "PROCESS_MODE_AUTO",
};

const Action = {
  PUSH: "ACTION_PUSH",
  PULL: "ACTION_PULL",
  NONE: "ACTION_NONE",
};

const Hand = {
  LEFT: "HAND_LEFT",
  RIGHT: "HAND_RIGHT",
  BOTH: "HAND_BOTH",
};

module.exports = {
  Action,
  Hand,
  ProcessMode,
};
